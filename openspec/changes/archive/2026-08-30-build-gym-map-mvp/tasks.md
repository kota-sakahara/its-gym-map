## 1. プロジェクト基盤

- [x] 1.1 Vite、Vanilla TypeScript、CSSによる最小構成を作り、MapLibre GL JSとCheerioだけを必要な外部依存として追加し、`npm ci`、テスト、プロダクションビルドが成功することを確認する
- [x] 1.2 公開カタログの型、4種類の契約経路、複数料金、出典、確認日を表すデータ形式を定義し、正常データを受理して必須項目欠落を拒否するNode.js標準テストで確認する
- [x] 1.3 同一座標に異なる契約経路の掲載を含む最小サンプルカタログを用意し、異なるIDのレコードとして読み込めることをテストで確認する

## 2. 決定論的なデータ取得

- [x] 2.1 Node.js標準 `fetch`、Cheerio、固定の文字列正規化と料金対応表による共通処理を実装し、同じHTML入力から常に同じ結果を生成することをfixtureテストで確認する
- [x] 2.2 ITSのへるすぴあページから施設情報と曜日別料金を抽出し、出典と確認日を持つ単一レコードになることをfixtureテストで確認する
- [x] 2.3 コナミスポーツクラブの公式施設情報とITS料金表から施設、カテゴリ、本人負担額を生成し、グランサイズを除外することをfixtureテストで確認する
- [x] 2.4 コナミ提携施設一覧から `気軽に都度利用(A)` の施設、住所、元料金を抽出してITS本人負担額へ変換し、A以外の料金を採用しないことをfixtureテストで確認する
- [x] 2.5 セントラルの公式クラブ一覧から `area=club_*` の都道府県別リンクを収集して各施設の名称、住所、利用料を抽出し、リンクの重複、都道府県欠落、未知の料金を検出するfixtureテストで確認する
- [x] 2.6 明示的なブランド別名表、`その他`への分類、契約経路を含む安定ID生成を実装し、同一施設でも契約経路が異なれば統合されないことをテストで確認する
- [x] 2.7 カタログ生成の依存関係と通信先にAIモデル、LLM、AI-OCR、AIサービスが含まれず、未知の値を推測せず失敗させることを依存一覧、コード確認、異常系テストで確認する

## 3. 座標化と公開カタログ生成

- [x] 3.1 住所をキーとするバージョン管理可能な座標キャッシュ、手動確定座標、公式座標の優先、国土地理院APIを主（最大20リクエスト/秒）・Nominatimを補助（最大1リクエスト/秒）とするジオコーディング、住所要素一致検証を実装し、キャッシュ済み住所では外部問い合わせが発生しないことをテストで確認する
- [x] 3.2 各ソースのレコードを契約経路単位で結合し、必須項目、ID、料金、緯度経度、出典、確認日、件数急減を検証して、不正データでは非0終了することをテストで確認する
- [x] 3.3 検証成功時だけ公開JSONと座標キャッシュを置き換える更新コマンドを実装し、意図的な取得・検証失敗で既存ファイルと確認日が変化しないことを統合テストで確認する
- [x] 3.4 自動座標化できない掲載について施設名、契約経路、元住所、施設情報の参照先、候補と棄却理由を出力する非公開の失敗レポートと、補正住所または確定座標、根拠URL、確認日、理由を持つバージョン管理可能な補正台帳を実装し、元住所の維持と未解決掲載の除外禁止をテストで確認する
- [x] 3.5 座標化途中で未解決住所が残った場合、検証済み座標をGit管理および公開対象外の一時キャッシュに保存して再実行で再利用し、全件検証成功時だけ正式キャッシュへ反映して一時キャッシュを削除することを統合テストで確認する
- [x] 3.6 今回未解決となった19住所を公式施設情報と照合して補正台帳へ根拠付きで記録し、公式ソースから初回カタログを生成して、契約経路別件数、料金変換、出典、代表施設の住所と座標を人手で照合して記録する

## 4. 地図と探索機能

- [x] 4.1 OpenFreeMapのDark系スタイルを使った全画面MapLibre地図、一覧ウィジェット、掲載件数と最終確認日のステータス表示を実装し、サンプルカタログが地図と一覧へ表示されることをブラウザで確認する
- [x] 4.2 現在地取得成功時は現在地、拒否・失敗・タイムアウト時は東京駅を初期基準地点とし、基準地点と最寄り施設が収まることを位置情報の許可・拒否両方で確認する
- [x] 4.3 MapLibreの距離計算による近い順表示と、クリックおよびドラッグ可能な基準ピン、基準ピン解除後の復帰処理を実装し、基準地点変更に合わせて距離と順序が更新されることを確認する
- [x] 4.4 1掲載1マーカーと一覧選択の同期、同一座標マーカーの画面上オフセットを実装し、異なる契約経路の同一座標レコードを個別に選択できることを確認する
- [x] 4.5 施設名、ブランド、住所、契約経路の部分一致検索と、ブランド・価格帯の絞り込みを実装し、複合条件、複数料金、0件状態をテストとブラウザ操作で確認する
- [x] 4.6 2D/3D切り替え、緯度経度に固定されたGeoJSON施設ピン、3D時に画面内の施設だけへ表示するガイド線とブランド名を実装し、傾きと方位を変更しても施設ピンとガイドの根元が対応地点へ追従することを確認する
- [x] 4.7 施設ピンと一覧行の選択から共通の非モーダル詳細カードを表示し、名称、ブランド、契約経路、全料金、距離、住所、公式リンク、閉じる操作、別施設への切り替え、モバイルでの一覧自動折り畳みを実装してテストとビルドで確認する

## 5. テレメトリーHUDデザイン

- [x] 5.1 半透明の暗色パネル、等幅フォント、細い罫線、シアン主体の状態色をCSSで実装し、地図を主領域とする管制室風レイアウトがデスクトップで成立することを確認する
- [x] 5.2 狭い画面で一覧を下部パネルへ切り替え、主要操作をキーボードで利用でき、色以外の状態表示、明瞭なフォーカス、`prefers-reduced-motion`が機能することをモバイル幅とアクセシビリティ確認で検証する
- [x] 5.3 最も古い参照先の確認日が7日を超えた場合に `DATA STALE` を表示し、日付境界を固定したテストで警告の有無を確認する
- [x] 5.4 地図上を巡回する走査ビームと、ズーム・方位・傾斜へ追従するライブ計器を追加し、操作部を動かさず`prefers-reduced-motion`で停止できることを確認する
- [x] 5.5 モバイル幅で一覧パネルと凡例を折り畳めるようにし、safe area、動的viewport、44px以上の主要タッチ領域、入力時の自動ズーム防止、軽量化した演出を実装してビルドで確認する

## 6. 自動更新と配信

- [x] 6.1 push、月曜・木曜JST 03:17の定期実行、手動実行に対応する単一GitHub Actions workflowを作り、テスト、定期時のカタログ更新、ビルド、GitHub Pages配信の依存順をworkflow構文確認で検証する
- [x] 6.2 定期更新成功時だけ公開JSONと座標キャッシュをコミットし、更新または検証失敗時はコミットとPages配信を行わない条件を実装し、成功・失敗を模したworkflow実行で確認する
- [x] 6.3 GitHub Pagesのリポジトリ配下URLでアセット、公開JSON、地図スタイルを読み込めるVite設定にし、ローカルのプロダクションpreviewとPages環境で404がないことを確認する

## 7. MVP受け入れ確認

- [x] 7.1 全テストとプロダクションビルドを実行し、決定論的な再生成、AI不使用、更新失敗時の正常データ維持をまとめて確認する
- [x] 7.2 現在地許可、現在地拒否、東京駅fallback、任意基準ピン、検索、複合絞り込み、同一座標の複数掲載をデスクトップとモバイル幅で操作し、各specのシナリオを満たすことを確認する
- [x] 7.3 手動のGitHub Actions更新からPages配信までを通し、公開サイトの掲載件数、料金、出典、最終確認日を代表サンプルについて公式情報と照合する
