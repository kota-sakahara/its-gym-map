{
  "name": "its-gym-map",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "test": "node --test",
    "update": "node scripts/update-catalog.mjs",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "cheerio": "^1.1.2",
    "maplibre-gl": "^5.7.1"
  },
  "devDependencies": {
    "typescript": "^5.9.2",
    "vite": "^7.1.3"
  },
  "engines": {
    "node": ">=24"
  }
}
